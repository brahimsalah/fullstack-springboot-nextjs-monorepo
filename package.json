{
  "name": "fullstack-springboot-nextjs-monorepo",
  "private": true,
  "packageManager": "pnpm@9",
  "scripts": {
    "dev": "concurrently -n api,web -c auto \"mvn -q -f apps/api/pom.xml spring-boot:run\" \"pnpm --dir apps/web dev\"",
    "build": "pnpm --dir apps/web build && mvn -q -f apps/api/pom.xml -DskipTests package",
    "start:web": "pnpm --dir apps/web start",
    "format": "prettier --write .",
    "lint": "pnpm --dir apps/web lint",
    "typecheck": "pnpm --dir apps/web typecheck"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.3.3"
  },
  "workspaces": [
    "apps/*"
  ]
}
